<!-- cs overview component -->
<section>
  <div class="page-width">
    <div class="cs-grow-3 cs-justify-between">
      <h1 class="cs-pad-sides">{{heading}}</h1>
      <div class="cs-flex cs-flex-row">
        <div class="cs-grow-1 cs-pad-sides">
          <h4>{{left-side.subheading}}</h4>
          <p>{{left-side.text}}</p>
        </div>
        <div class="cs-grow-1 cs-pad-sides">
          <h4>{{right-side.subheading}}</h4>
          <p>{{right-side.text}}</p>
        </div>
      </div>
    </div>
  </div>
</section>
